{
  "name": "roxy-tests",
  "private": true,
  "description": "Headless Playwright verification suite for Roxy.JS. Run: npm i && node verify.js (expects Chromium at /opt/pw-browsers, see verify.js executablePath).",
  "scripts": {
    "test": "node verify.js"
  },
  "dependencies": {
    "playwright-core": "^1.61.1",
    "three": "^0.128.0"
  }
}
